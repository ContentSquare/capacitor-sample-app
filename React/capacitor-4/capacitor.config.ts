import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.contentsquare.sampleapp.capacitor.react',
  appName: 'capacitor-sample-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
